# Local dev: leave unset — defaults to http://localhost:8000/api
#
# Production on Vercel (Services model): set this to the relative path
# "/api" — the frontend and backend are served from the same domain, so a
# relative path avoids CORS entirely.
VITE_API_BASE_URL=
